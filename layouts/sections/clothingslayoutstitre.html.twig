{% if content %}
	<div {{attributes.addClass('title-indicator')}}>
		{% if content.title %}
			<{{settings.region_tag_title}} {{region_attributes.title.addClass(['title','cna-title'])}}>
				{{content.title}}
			</{{settings.region_tag_title}}>
			<div class="sm-indiactor"></div>
		{% endif %}
	</div>
{% endif %}